# PepeKeyboard 🐸⌨️

Супер-легковесная программа, которая воспроизводит кастомные звуки при нажатии клавиш на клавиатуре. Работает в фоне и имеет иконку в системном трее.

## Особенности
- 🪶 **Легковесность**: Написана на Rust, потребляет минимум ресурсов процессора и оперативной памяти.
- 🎵 **Кастомные звуки**: Разные звуки для обычных клавиш (выбираются случайно) и специальные звуки для `Enter` и `Cmd/Win`.
- 🌍 **Кроссплатформенность**: Поддерживает macOS, Windows и Linux.
- 👻 **Фоновая работа**: Прячется в системный трей (Menu Bar на macOS).

## Установка

### Шаг 1. Установка зависимостей
Для сборки программы вам понадобится язык программирования Rust.
- **Windows / macOS / Linux**: Установите Rust с официального сайта: [rustup.rs](https://rustup.rs/) (или выполните команду `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh` в терминале).

**Дополнительные зависимости для Linux (Ubuntu/Debian):**
Для работы со звуком и перехвата клавиатуры на Linux потребуются системные библиотеки:
```bash
sudo apt update
sudo apt install pkg-config libasound2-dev libx11-dev libxtst-dev
```

### Шаг 2. Скачивание исходного кода
Склонируйте репозиторий на свой компьютер и перейдите в папку с проектом:
```bash
git clone https://github.com/rTexty/GunwestKeyboard.git
cd GunwestKeyboard
```

### Шаг 3. Сборка и запуск

#### 🍏 macOS
На macOS программа должна быть упакована в `.app` бандл, чтобы операционная система могла корректно выдать разрешения на глобальный перехват нажатий клавиатуры (Accessibility).

1. Запустите скрипт сборки:
   ```bash
   ./package.sh
   ```
2. В папке проекта появится файл `PepeKeyboard.app`. Переместите его в папку "Программы" (Applications).
3. Запустите `PepeKeyboard.app`.
4. **Важно**: При первом запуске macOS попросит дать разрешение на "Универсальный доступ" (Accessibility). Разрешите это в `Настройки -> Конфиденциальность и безопасность -> Универсальный доступ` (Privacy & Security -> Accessibility), добавив программу в список.

#### 🪟 Windows / 🐧 Linux
На Windows и Linux программа работает как обычный портативный исполняемый файл.

1. Соберите релизную версию программы:
   ```bash
   cargo build --release
   ```
2. Исполняемый файл появится по пути `target/release/PepeKeyboard` (или `target\release\PepeKeyboard.exe` на Windows).
3. Создайте отдельную папку для программы в любом удобном месте.
4. Скопируйте туда исполняемый файл `PepeKeyboard`.
5. **Важно**: Скопируйте все `.wav` файлы (`click1.wav` - `click4.wav`, `enter.wav`, `cmd.wav`) в ту же папку, где теперь находится исполняемый файл.
6. Запустите исполняемый файл.

## Кастомизация звуков
Вы можете заменить стандартные звуки на свои. Для этого просто замените `.wav` файлы в корне проекта (или в папке `PepeKeyboard.app/Contents/Resources` на macOS):
- `click1.wav` ... `click4.wav` — звуки для обычных клавиш (выбираются случайно).
- `enter.wav` — звук для клавиши Enter.
- `cmd.wav` — звук для клавиши Cmd (macOS) или Win (Windows).

*Примечание: Файлы должны быть строго в формате `.wav`.*
