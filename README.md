# PepeKeyboard 🐸⌨️

Супер-легковесная программа, которая воспроизводит кастомные звуки при нажатии клавиш на клавиатуре. Работает в фоне и имеет иконку в системном трее.

## Особенности
- 🪶 **Легковесность**: Написана на Rust, потребляет минимум ресурсов процессора и оперативной памяти.
- 🎵 **Кастомные звуки**: Разные звуки для обычных клавиш (выбираются случайно) и специальные звуки для `Enter` и `Cmd/Win`.
- 🌍 **Кроссплатформенность**: Поддерживает macOS, Windows и Linux.
- 👻 **Фоновая работа**: Прячется в системный трей (Menu Bar на macOS).

## Требования
- Установленный [Rust и Cargo](https://rustup.rs/)

## Установка и запуск

### 🍏 macOS
На macOS программа должна быть упакована в `.app` бандл, чтобы операционная система могла корректно выдать разрешения на глобальный перехват нажатий клавиатуры (Accessibility).

1. Откройте терминал в папке с проектом.
2. Запустите скрипт сборки:
   ```bash
   ./package.sh
   ```
3. В папке проекта появится файл `PepeKeyboard.app`. Вы можете переместить его в папку "Программы" (Applications) или запускать прямо отсюда.
4. **Важно**: При первом запуске macOS попросит дать разрешение на "Универсальный доступ" (Accessibility). Разрешите это в `Настройки -> Конфиденциальность и безопасность -> Универсальный доступ`.

### 🪟 Windows / 🐧 Linux
На Windows и Linux программа работает как обычный исполняемый файл.

1. Откройте терминал/командную строку в папке с проектом.
2. Соберите релизную версию:
   ```bash
   cargo build --release
   ```
3. Исполняемый файл появится по пути `target/release/PepeKeyboard` (или `PepeKeyboard.exe` на Windows).
4. Скопируйте этот файл в любую удобную папку.
5. **Важно**: Скопируйте все `.wav` файлы (`click1.wav` - `click4.wav`, `enter.wav`, `cmd.wav`) в ту же папку, откуда будете запускать программу (или запускайте через `cargo run` прямо из корня проекта).
6. Запустите исполняемый файл.

## Кастомизация звуков
Вы можете заменить стандартные звуки на свои. Для этого просто замените `.wav` файлы в корне проекта (или в папке `PepeKeyboard.app/Contents/Resources` на macOS):
- `click1.wav` ... `click4.wav` — звуки для обычных клавиш (выбираются случайно).
- `enter.wav` — звук для клавиши Enter.
- `cmd.wav` — звук для клавиши Cmd (macOS) или Win (Windows).

*Примечание: Файлы должны быть строго в формате `.wav`.*
